# ---- PUBLIC (safe to expose to the browser) ----
NEXT_PUBLIC_SUPABASE_URL=https://rewxffzzruwdbketqway.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_HtV3LsVCGToYAlEMwg6mUg_r1qzGEzw

# ---- SERVER ONLY — never prefix with NEXT_PUBLIC_ ----
# Google Drive API key (used to list videos in a public Drive folder).
GOOGLE_DRIVE_API_KEY=your-google-drive-api-key-here

# Supabase service-role key — needed for email notifications (admin API).
# Find it in: Supabase Dashboard > Project Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Email notifications (Resend — free tier at https://resend.com).
# Add a verified sender domain in Resend, then copy your API key here.
RESEND_API_KEY=your-resend-api-key-here
EMAIL_FROM=Feedback Dashboard <no-reply@yourdomain.com>

# Telegram is no longer used (kept here only for reference; safe to delete).
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
